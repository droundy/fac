[package]
name = "fac"
version = "0.1.0"
authors = ["daveroundy@gmail.com"]
build = "build.rs"

[dependencies]

libc = "^0.2"
typed-arena = "1.2.0"

[build-dependencies]
gcc = "0.3"

[features]
# Treat warnings as a build error.
strict = []
