[package]
name = "fac"
version = "0.1.0"
authors = ["daveroundy@gmail.com"]

[lib]
name = "fac"
path = "src/lib.rs"

[[bin]]
name = "fac"
path = "src/main.rs"
doc = false

[dependencies]

libc = "^0.2"
typed-arena = "1.2.0"
bigbro = "^0.2.0"

[dev-dependencies]
quickcheck = "^0.4.1"

[build-dependencies]
gcc = "0.3"

[features]
# Treat warnings as a build error.
strict = []
